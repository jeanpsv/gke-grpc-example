apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: apps
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  selector:
    app: backend
